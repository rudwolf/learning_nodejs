<!DOCTYPE html>
	<html lang="en">
		<head>
			<meta charset="UTF-8">
			<title>Formulário de cadastro de Notícia</title>
		</head>
	<body>

		<h1>Adicionar notícia</h1>

        <div class="errors">
            <%
            if(validation) { %>
            <ul>
                <% for(var i = 0; i < validation.length; i++) { %>
                <li>
                    <%= validation[i].msg %>
                </li>
                <% } %>
            </ul>                
            <%
            } %>
        </div>

        <div>
            <form action="/news/save" method="post">
                <div>
                    <label for="title">Title</label>
                    <input type="text" name="title" value="<%=news_body.title%>" id="title">
                </div>
                <div>
                    <label for="title">Resumo</label>
                    <input type="text" name="excerpt" value="<%=news_body.excerpt%>" id="excerpt">
                </div>
                <div>
                    <label for="title">Autor</label>
                    <input type="text" name="author" value="<%=news_body.author%>" id="excerpt">
                </div>
                <div>
                    <label for="title">Publicado em</label>
                    <input type="date" name="published_at" id="published_at" value="<%=news_body.published_at%>">
                </div>
                <div>
                    <label for="text">Notícia</label>
                    <textarea name="text" id="text" cols="30" rows="10"><%=news_body.text%></textarea>
                </div>
                <div><input type="submit" value="Enviar"></div>
            </form>
        </div>

	</body>
</html>